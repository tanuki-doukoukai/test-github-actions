name: Concurrent Workflow 2

on:
  push:
    branches:
      - 'auto/**'
  workflow_dispatch:

jobs:
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Workflow 2
        run: |
          echo "Starting Concurrent Workflow 2"
          echo "Current time: $(date)"
          sleep 15
          echo "Workflow 2 completed"
